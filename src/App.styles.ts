import styled from 'styled-components';
import IconButton from '@mui/material/IconButton';

export const Wrapper = styled.div`
  width: 100%;
  max-width: 1280px;
  margin: auto;
`;

export const SidebarButtonBox = styled.div`
    position: fixed;
    right: 20px;
    top: 20px;
`;

export const StyledButton = styled(IconButton)`
    
`;
